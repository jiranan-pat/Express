extends layout

block content
  h1= award.type
  h5 Winner is #{award_winner}
  p= award.detail
  hr
  if user
    if user.id ==award.award_winner
      a.btn.btn-primary(href='/awards/edit/'+award._id) Edit
      a.btn.btn-danger.delete-award(href="#", data-id=award._id) Delete
